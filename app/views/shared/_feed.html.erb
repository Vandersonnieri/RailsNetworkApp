<% if @feed_items.any? %>
    <ol class="microposts">
        <%# rails know to call partial microposts because 
        because elements of feed_item is of Microposts class %>
        <%= render @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
<% end %>